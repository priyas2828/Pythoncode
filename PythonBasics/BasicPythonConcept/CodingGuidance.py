# Read data from the user so we are going to use input method
# Python code need not to write semicolon , we can skip that
# Continuation symbol is we can write it in next ine using backslash \
# In one line we can write the code using ;


readData = \
    input("Enter your name - ")
print(readData)

readData1 = input("Enter some value - ") ;print(readData1)
